# 同花顺多空信号自动交易系统 · 系统功能报告（最新版）

生成时间：2026-09-16　|　版本：十模型评审第二轮整改 + 尾盘竞价顶格报价规则后（8 项冲突+新增尾盘规则全部经用户裁定落地，selftest 224/224 全绿）　|　数据来源：真机/真账户实测 + 代码事实，无估算项另行标注

---

## 一、系统概述

一套运行在 Windows PC + Android 手机上的 **A 股无人值守信号交易系统**：通过 ADB 操作手机端同花顺 App，逐只读取"我的自选"股票日 K 线图上的"多/空"角标信号；PC 端同花顺行情端以用户自定义快捷键（F1/F3）闪电下单，F6 浮层验证成交，全程风控、告警、对账、自检闭环。

**每日运行节奏**：盘前一键自检（阻断项不过拒绝开盘）→ 9:25 前完成首轮全量扫描 → 全天每 4–5 分钟一轮滚动扫描，持仓股 90 秒插扫 → 14:57 尾盘集合竞价兜底结算 → 15:00 后排队单自动成交复查 → 收盘可复盘。当前执行模式为 **paper 模拟盘**，配置一键可切 auto 实盘。

**最新交易规则（2026-09-12 用户逐条裁定）**：

1. 空头信号优先级最高，持仓见空必卖（后续再叠加其他卖出逻辑，不替代空头）；
2. 涨停见多头不排队买入、只监控开板机会；跌停见空头必须排队卖出，两种情况均告警，封单大减最高级别处置；
3. 集合竞价（9:15–9:25）不交易；首轮扫描在 9:25–9:30 完成，此窗口挂单未成交等待 **5 分钟**，其余时段 **2 分钟**；
4. 买入 F1（最新价、账户资金 25%），卖出 F3（最新价、清仓，不核卖）；第二次同向信号重挂有价格保护（买≤首价、卖≥首价）；全天未成交 14:57 尾盘集合竞价兜底，**结算单顶格报价（买报涨停价/卖报跌停价自定义键，按收盘价撮合，2026-09-16 新增）**；
5. 信号不排队、不做时效衰减——信号持续存在且价格回落视为上车机会；
6. 仓位语义固定：买 25%、卖清仓，**绝不按股数**（由同花顺客户端热键侧定义）；
7. 持仓股永久钉在自选列表，不随热榜换血；持仓股查卖出逻辑、自选股查买入逻辑。

**代码规模**：生产+测试代码约 **1.38 万行**（最大模块 [hotkey_trader.py](file:///d:/Awakening/trader/hotkey_trader.py) 1507 行，回归测试集 1332 行 / 224 项断言）；33 个一次性诊断脚本归档于 `experiments/`；零重型依赖（视觉用 OpenCV、PC 窗口用 pywinauto、Web 台为标准库 http.server，两轮加固+尾盘顶格规则全部零新依赖，新增逻辑只用标准库 uuid/time/json/subprocess/collections）。

---

## 二、总体架构

### 2.1 物理拓扑

```
┌─────────────────────────────── Windows PC ───────────────────────────────┐
│                                                                          │
│  main.py auto_round (主控循环)                                           │
│   ├─ instance_lock       单实例锁(Win互斥量/POSIX flock, 防多开双发键)   │
│   ├─ scanner.scheduler   轮调度/持仓插扫/尾盘14:57结算                     │
│   ├─ detector*           截图模板匹配(多空角标/代码数字/F6持仓)           │
│   ├─ decision            信号×持仓×台账 → 买卖决策                        │
│   ├─ trader              HotkeyTrader(F1/F3/F8) + 风控 + 封单监控         │
│   ├─ monitor             心跳看门狗/封单监控/大盘买侧熔断/资源守卫/日报    │
│   ├─ webapp              127.0.0.1:8899 可视化台(11页签, 独立进程可同跑)  │
│   └─ notify              企业微信/飞书机器人推送                          │
│         │ ADB(USB)                    │ HTTP               │ pywinauto    │
│         ▼                              ▼                    ▼             │
│   Android 手机同花顺App       腾讯/新浪双源行情+热榜API   PC同花顺hexin.exe  │
│   (截图+点击翻页)                (实时价/盘口/封单)    (热键下单/F6浮层)   │
│                                                            │             │
│                                                     xiadan.exe(仅回查/兜底)│
└──────────────────────────────────────────────────────────────────────────┘
```

### 2.2 模块职责

| 包/文件 | 职责 |
|---|---|
| [main.py](file:///d:/Awakening/main.py) | CLI 入口：auto_round / scan 系列 / preflight / web / manual_audit / cleanup / watchdog |
| [adb/](file:///d:/Awakening/adb) | 设备枚举（含 adb server 自愈）、UIController 点击输入、PNG 截图（0.7s/帧） |
| [detector/](file:///d:/Awakening/detector) | OpenCV 模板匹配：多/空角标（阈值 0.70）、数字模板读代码、UI 层级检测、F6 无持仓识别 |
| [ths/](file:///d:/Awakening/ths) | 热榜抓取、云自选同步、导航器（自选页/日K/goto 恢复）、腾讯/新浪双源实时行情与盘口、市场概貌（[market.py](file:///d:/Awakening/ths/market.py) 指数/涨跌家数/板块，东财多子域轮试）、标的过滤、Cookie 保活 |
| [scanner/](file:///d:/Awakening/scanner) | 主轮扫描循环、自动轮调度器、持仓插扫、首次信号记录、计时与 CSV |
| [decision/](file:///d:/Awakening/decision/decision.py) | 决策引擎：涨跌停分支、二次挂单价格条件、满仓记录、queue_only 动作 |
| [trader/](file:///d:/Awakening/trader) | HotkeyTrader（主通道）、RiskController、PaperTrader、xiadan 表单兜底、PC 窗口识别、人工对账 |
| [monitor/](file:///d:/Awakening/monitor) | 心跳+看门狗、封单监控 SealWatcher（含 14:57 收盘竞价 F8 硬门禁）、午盘/收盘日报、大盘买侧熔断 MarketGuard、运行资源守卫 ResourceGuard |
| [notify/](file:///d:/Awakening/notify/notifier.py) | 企业微信/飞书/钉钉/Server酱四通道，P0/P1/P2 分级与去重 |
| [models/](file:///d:/Awakening/models) | 持仓事实源（原子写+损坏隔离+快照/事件自愈重建）、结果模型、事件审计 JSONL |
| [instance_lock.py](file:///d:/Awakening/instance_lock.py) | 跨进程单实例锁：Windows 命名互斥量 / POSIX flock，进程死亡 OS 自动释放，只锁 auto_round 与 pipeline |
| [webapp/](file:///d:/Awakening/webapp/app.py) | 标准库 HTTP 服务 + 单页前端（11 页签，CSRF 防护） |
| [preflight.py](file:///d:/Awakening/preflight.py) | 盘前完整自检 + 轻量健康探活（两套） |
| [pipeline_round.py](file:///d:/Awakening/pipeline_round.py) / [verify_round.py](file:///d:/Awakening/verify_round.py) | 生产对齐流程脚本 / 人工核对轮（逐只存截图） |
| [keepawake.py](file:///d:/Awakening/keepawake.py) | 防休眠熄屏 + 屏保进程检测 |
| [cleanup/cleaner.py](file:///d:/Awakening/cleanup/cleaner.py) | TTL 垃圾清理（无数据库） |

### 2.3 一轮 auto_round 数据流

热榜 API → 主板/ST/次新过滤 →（续扫探测：在自选循环页则免重进，省 ~17s）→ 主轮沿 `>` 翻页逐只截图识别，每 90s 插入持仓巡检 → 信号实时进决策引擎 → 热键下单+F6 验证 → 轮末云同步 `热榜∪持仓` → 热榜快照落盘 → 轮间隔自适应等待（片间巡检/日报/14:57 结算守卫）。

---

## 三、交易决策子系统（核心）

### 3.1 信号语义（2026-09-11 经争议后裁定）

- 角标锚定在**所属 K 线**上（可在 K 线上方或下方），图上同时存在多日历史角标；
- 系统**只读最新一根 K 线（中心 x=1026±16，右缘 120px 整列、与主图等高）**上的角标 = 当日新信号；历史角标（如倒数第二根 K 上昨日的角标）被中心带排除是设计行为；
- 输出四态：`long 多 / short 空 / none 无 / unknown 未知`，均带置信度；**unknown 永不允许降级为"无"而静默**，持仓股 unknown 必告警。

### 3.2 买入决策（未持仓 + 多）

按以下顺序短路判定（[decision.py](file:///d:/Awakening/decision/decision.py#L124-L180)）：

1. **满仓**（已持 max_positions=4 只）→ 不交易，记录信号时间+现价（full_signal 审计事件，每股当日仅推送一次）；扫描永不停止；
2. **标的防御**：非沪深主板 / ST 名称 → 拒绝（前置过滤之外的零成本双保险）；
3. **已有买单 pending**（挂单中）→ 忽略，绝不重复发单；
4. **涨停封板** → 不排队买入：注册 SealWatcher.watch_up，当日一次 WARNING 告警，等开板/封单大减人工补单；
5. **二次挂单价格保护**：上笔 BUY 台账为 canceled 且现价 > 首次挂单价 + 0.005 → IGNORE（不追高）；现价 ≤ 首价 → BUY（价格回落=上车机会）；
6. 首次多头（无台账）→ 直接 BUY，不查价格；
7. BUY 动作经 trader 走风控预检（kill_switch / 每股每日成交限买 1 次 / 交易时段 09:25 起 / 日单量 / 数量）→ F1 热键 → F6 轮询验证。

### 3.3 卖出决策（持仓 + 空）

空头信号为**最高优先级卖出信号**（[decision.py](file:///d:/Awakening/decision/decision.py#L182-L220)）：

1. 已有卖单 pending（含跌停排队）→ 不重复挂；
2. **跌停封板** → SELL(queue_only=True) 排队卖出（F3 最新价在封死时等同跌停价排队），注册 watch_down 最高级别监控；
3. 二次挂单价格保护：上笔 SELL canceled 且现价 < 首价 − 0.005 → 不低价重挂（等反弹或尾盘竞价）；现价 ≥ 首价 → 重挂；
4. 首次空头 → 直接 F3 卖出清仓；
5. **T+1 硬约束**：positions.json 中 entry_time 为今日的股票风控直接拒卖（读取异常不阻断，由券商端最终校验）；
6. 持仓 + 多/无信号 → HOLD（只要不出空就一直持有）。

### 3.4 挂单生命周期台账（trade_state.json → attempts）

每笔发出的热键委托记录 `{code: {BUY/SELL: {price, ts, status}}}`，进程重启后仍生效（[risk_control.py](file:///d:/Awakening/trader/risk_control.py#L161-L204)）：

```
发键成功 → pending ──F6成交──→ filled（BUY 占每股每日名额）
                   ├─2/5分钟到期未成交─→ F8撤单 → canceled（不占买入名额；下轮受价格保护）
                   ├─queue_only排队单 ─→ 留单 ──15:00撮合──→ filled / canceled(闭市废单)
                   ├─封单大减(跌停) ───→ F8撤单 → canceled + 人工接管
                   └─F6链路异常 ───────→ unknown + CRITICAL（不补账不撤单）
```

关键设计：**撤单不消耗每股每日买入名额**（只有 filled 计），二次挂单由决策层价格条件把关，兼顾"不追高"与"跌回首价可补"。

**2026-09-16 第二轮收紧**：① 每笔新发委托分配幂等键 `cid=YYYYMMDD-code-ACTION-seq`（买卖方向各自增、当日持久化，重挂/撤后再发是一笔新委托→新 cid，状态补正沿用原 cid）；② 合法状态机显式化——pending 可转任意态，canceled/unknown 允许补正 filled（延迟成交/收盘复查），**filled 是终态不可回退**（回退会凭空释放每股每日限买名额，非法流转拒绝并 CRITICAL），同态重复（如 filled→filled 双记账）幂等静默；③ **单票每日重挂上限 3 次**（用户裁定，首次挂单不计、买卖分别计数，达上限规则性拒单，queue_only 结算单旁路；0=关闭）。

### 3.5 涨跌停特殊处置（[seal_watcher.py](file:///d:/Awakening/monitor/seal_watcher.py)）

- 数据源：行情双源冗余（[quote.py](file:///d:/Awakening/ths/quote.py)，腾讯主 / 新浪备，主源失败自动切换，快照带 `source` 标记；新浪需带 Referer 头，涨跌停价按 ±10%/ST ±5% 四舍五入到分本地计算），买一/卖一价量、涨停价、封单手数，10 秒轮询，纯 PC HTTP 不占手机通道；
- **停牌识别**：快照 `halted` 标志（现价≤0 且有昨收，或价=昨收且开高低全 0）。决策层在涨跌停判定**之前**拦截：多头遇停牌→IGNORE+WARNING 不挂买单；持仓空头遇停牌→IGNORE+CRITICAL（卖不出，复牌后首轮重判）；尾盘结算的买/卖两支同样拦截；当日告警去重，复牌信号仍在自动恢复交易；
- 封板判定：现价=涨跌停价（eps=0.005）且对手盘挂单量为 0；
- **封单秒级异动（2026-09-12 用户重定义，取代旧 50% 缩量规则）**：10s 轮询，封单量环比变动 **≥10%**（基数 <50 手不判，防低流动性噪声）→ 立即告警：骤减 CRITICAL（可能开板）、暴增 WARNING（封板加强），同票纯告警 180s 冷却防刷屏；**异动时若台账存在该股 pending 挂单（买/卖都算）→ 必须持窗口锁 F8 撤单**，每票只撤一次且不受冷却限制，撤单成功台账对应方向转 canceled；
- **涨停 watch_up**：开板（卖一重新有量或离开涨停价）一次性告警并摘除监控；任何涨停事件**只告警不自动补单买入**（人工判断，用户明确策略待校验丰富后再写入）；
- **跌停 watch_down**：开板或封单异动 → 排队卖单按上面规则 F8 撤 → 告警人工以更好价格卖；撤单成功台账转 canceled，15:00 复查线程据此跳过（不与人工操作冲突）；
- **收盘集合竞价硬门禁（2026-09-15 用户裁定，合规层不可绕过）**：工作日 **14:57:00–15:00:00**（沪深一致）交易所禁止撤单，F8 强发即废单。该窗口内一切封单异动（开板/骤减）只告警：系统硬拦截 F8、不设 `cancelled`、挂单原样保留到 15:00 收盘对账，每票推送一条 CRITICAL"收盘竞价禁撤·留单"；14:56:59 及以前、周末均不受门禁影响。

### 3.6 三个交易时间窗口

| 窗口 | 规则 |
|---|---|
| 9:15–9:25 集合竞价 | 风控拒单（sessions 自 09:25 起） |
| 9:25–9:30 开盘窗口 | 首轮扫描在此完成并挂单；挂单未成交等待 **opening_pending_wait=300s**（全天最活跃时段，给足成交时间） |
| 9:30–14:57 连续竞价 | 常规挂单 **buy_pending_wait=120s** 未成交即 F8 撤单+告警；**14:55:00 起停止新主动买单**（用户裁定，含二次挂买；卖单/止损/F8/14:57 queue_only 结算单不受影响） |
| 14:57–15:00 尾盘集合竞价 | 主循环 14:57:00 deadline 中断点（持仓插扫之前生效，`should_break` 回调，中断轮跳过覆盖率审计/云同步直接返回），随后立即 `maybe_closing_settle` 兜底结算（见下）；**结算单顶格报价：买报涨停价/卖报跌停价（用户自定义热键，按收盘价统一撮合只为拿优先级）**；**该窗口交易所禁止撤单，封单监控 F8 硬门禁生效：只告警+留单到 15:00 对账，绝不发废单** |

**尾盘结算逻辑**（[scheduler.py](file:///d:/Awakening/scanner/scheduler.py#L329-L480)）：枚举台账中 canceled 的票——
- 卖：仍持仓且重新扫描信号为空/无 → 强制挂 queue_only 竞价卖（信号转"多"说明反弹则续持；UNKNOWN 绝不自动卖转告警）；
- 买：未持仓、仍为多头、未涨停封板、且 `剩余槽位=4-持仓数>0` → queue_only 竞价买（槽位计数防竞价同时成交超限）；
- 触发点：主循环轮头 + 轮间 idle 每 20 秒分片双守卫；异常不打完成标记，窗口内自动重试；
- **顶格报价（2026-09-16 用户裁定新增规则）**：尾盘结算单（仅 closing_auction 单，盘中跌停排队卖不受影响）改发用户在同花顺面板自定义的"涨停价买入/跌停价卖出"键（`closing_buy_limit_key`/`closing_sell_limit_key`，如 {F9}/{F10}）：集合竞价按收盘价统一撮合，顶格只决定撮合优先级而非实际成交价，数量仍由客户端决定（F1=25%/F3=清仓语义不变，系统不算股数）；键位未配置时回退常规 F1/F3 并按方向 WARNING 一次（不阻断结算），schema 校验键位格式；**物理例外不保证成交**——涨停封板买单仍不挂（跳过+开板告警人工补），跌停封板卖单维持排队+封单监控；
- queue_only 单 15:00 撮合，收盘复查线程 15:00:05 F6 补账（补建仓/补清仓+推送；未成交转 canceled 告之明日人工决策；检测异常 unknown+CRITICAL，次日启动恢复门兜底）。

---

## 四、信号采集子系统

### 4.1 手机端扫描

- **截图路线**：ADB PNG 0.7s/帧 + OpenCV 颜色模板匹配 9ms；UI dump（约 4s/次）仅用于低频导航，热路径零 dump；
- **主轮**：从当前自选股沿 `>` 按钮遍历，**回到起点即止**（适配热榜动态列表），起点 unknown 时以首个有效码补设；
- **三层持仓优先级**：① 重建轮轮头 goto 逐只巡检持仓；② 主轮每 90s 插扫全部持仓（插扫后经 enter_watchlist 恢复循环上下文）；③ 轮间等待期 idle 巡检（距上次 <90s 自动去重）；
- **续扫优化**：页面仍在自选 `>` 循环页则免重进（省 ~17s），连续 3 轮强制重建一次以纳入新自选；
- 切页三级处置：超时重试 1 次 → 重建扫描页 → 连续 3 只失败中止本轮并告警；设备掉线 15s 轮询重连、600s 上限，掉线/超时均手机推送。

### 4.2 检测器细节

- 检测区域：最新 K 线带 [960,700,1080,1700]（与主图等高——窄 y 边界曾导致"多"角标漏检，已扩）；模板阈值 **0.70**（0.80 会漏 0.71–0.79 的真信号，09-10 三轮 284 帧核对检测器全对，阈值不可降）；
- 代码读取：标题行白色数字模板匹配替代 UI dump，单只省 3.7s；
- goto 落页：`ensure_kline_page()` UI dump 动态定位"日K"tab + selected 校验（盘后横条会把 tab 从 y664 挤到 y772，硬坐标曾致持仓巡检静默全 NONE，09-11 修复）；最终无法确认抛 AdbError → 记 UNKNOWN 绝不静默 NONE；
- 颜色掩码兜底已关闭（K 线红绿柱体致 14/15 误报）。

### 4.3 首次信号快照

[first_signal.py](file:///d:/Awakening/scanner/first_signal.py)：每只股票当日首次判为多/空的瞬间记录 `{ts, code, name, dir, price, prev_close, pct}`，(code,dir) 当日去重，进程重启从当日 JSONL 重建不重复；价格走 PC 侧腾讯行情（与检测差约 1s，不占手机通道）；落 `logs/first_signal_YYYYMMDD.jsonl`，覆盖主轮/goto 巡检/test_current 全部路径，内部全兜底不影响扫描。

### 4.4 性能实测

| 指标 | 数据 |
|---|---|
| 一轮扫描（09-12 00:04 生产对齐流程） | 88 只全部成功，全程 **232s** |
| 盘中一轮（含交易/云同步） | 约 **4.8 分钟**（满足 5 分钟硬约束） |
| 单只识别均值 | 0.21s（P50=0.21，P95=0.23） |
| 盘中买单全链路 | 约 8–10s/只（信号时价 0.6s + 定位 2.5s + 发键 0.6s + F6 成交即返回 4–7s） |
| F8 单只撤单 / F5 全撤 | 4.1s / 1.5s |
| 轮间隔自适应 | 持仓≥3 只 → 间隔−120s（下限 180s）；≥1 只 → −60s（下限 240s）；空仓 300s |

---

## 五、选股与自选同步

- **热榜**：同花顺热榜 API（小时榜快照，与 App 同源），失败兜底 `logs/hotlist_local.json`；
- **前置过滤**（[universe_filter.py](file:///d:/Awakening/ths/universe_filter.py)）：热榜拉取后、云同步前过滤——仅沪深主板（60/00 开头，排 688/300/8·4·920 北交）+ 非 ST + 非次新（日 K <250 根≈1 年；8 线程并行首轮 3s，当日缓存后续轮 0.01s；查询失败容错放行）；**持仓股不受过滤**；09-12 实测 100 → 88 只；
- **云同步**：Cookie 调 Web 接口把"我的自选"整表同步为 **热榜∪持仓**（持仓跌出热榜也不删，钉死在自选）；同步置于轮末，列表变更零打断行情页；CookieKeeper 每 30 分钟访问续期，程序运行期基本不过期；
- **删除护栏（2026-09-16 用户裁定，[watchlist.py](file:///d:/Awakening/ths/watchlist.py)）**：v1 整表替换与 v2 逐条增删两条删除路径**之前**统一校验——待删除数占现有云自选比例 >30%（可配 `delete_guard_max_pct`）即抛专用异常中止同步、不触达任何删除接口，scheduler/pipeline 捕获后写 anomaly 审计 + CRITICAL 推送（防 Cookie 失效返回空列表/接口改版把云自选清空）；持仓股本身钉死不删；护栏可关；
- 与用户手机热榜"对不上"的三因已查明：主板过滤剔除、自选是上轮旧榜（盘中换血 +25/−32）、核对轮故意跳过云同步。

---

## 六、下单执行子系统

### 6.1 热键通道（主，[hotkey_trader.py](file:///d:/Awakening/trader/hotkey_trader.py)）

| 键 | 用户定义语义 |
|---|---|
| F1 | 最新价买入账户资金 **25%**（buy_variant=latest） |
| F2 | 卖一价买 25%（极端备用，ask1） |
| F3 | 最新价**清仓卖出**（sell_variant=latest，2026-09-12 起替代 F4） |
| F4 | 买一价核卖清仓（保留配置，不再默认） |
| F5 | 撤销全部委托——仅系统级异常（熔断/掉线）与盘后清理 |
| F6 | 查个股持仓浮层（成交验证，不弹 xiadan 无验证码） |
| F8 | 撤销**当前股票**挂单——挂单到期/封单大减用，防误撤其他票（2026-09-12 分级） |

- 仓位 25%/清仓在**客户端侧定义**，系统只发键、忽略 qty/price，天然不按股数；
- 下单前：屏保检测（ScreenSaverPlayer 必须退出）→ 每日 F12 bootstrap 登录（日期幂等）→ hexin 连接 → 快捷键面板像素检测（不可见自动点击唤出）→ **买入前 F6 预检**（券商侧已有持仓=疑似人工买入，拒单防重复建仓）→ 键盘精灵定位失败不耗买入名额；
- **F6 成交验证**：4s 首查 + 5s 轮询，成交立即返回（BUY 30s/SELL 12s 上限）；三态结果（无持仓/得分/链路 ok），链路异常不补不撤只告警；
- queue_only 单仅首查一次（竞价前不可能成交），直接挂 15:00 复查；
- **窗口锁**：主轮交易、挂单后台监控、封单撤单、人工对账全部持 `_win_lock` 串行，防热键发错股票/F5 误撤；
- form 兜底：面板唤不出时降级 xiadan 表单（可能弹验证码，30s 自动取消；qty 按 100 股保守，因 25% 语义在表单通道无法复现）。

**2026-09-16 第二轮加固（均默认开启、可 flag 回退）**：

- **卖出侧 F6 预检（用户裁定）**：与买入对称——发 F3 前 F6 核对券商侧确有持仓，无持仓拒卖（防热键发空/发错票），检测异常 fail-open 放行（券商端最终校验）；
- **发键前价格守卫（用户裁定"先只告警+采集滑点"）**：发键瞬间取实时价（而非信号产生时的旧价），买现价高于信号价 +0.8% / 卖出现价低于信号价 −1.5% 即 WARNING 推送 + `price_guard` 事件（含信号价/发键价/偏离率），**照常发单**；未越限的正常单发 `exec_quality` 滑点台账样本；`price_guard_enforce=true` 预留为将来硬拦；queue_only 排队单/结算单与取价失败一律跳过（fail-open）；
- **F8 撤单前后核对（用户裁定"增加，失败只告警"）**：撤单前读 hexin 主窗标题正则抽 6 位代码与目标票核对（pcwin 无读行情窗代码能力，抽不到 fail-open），撤单后 F6 验证委托确实消失；任一失败只 CRITICAL 人工介入，**绝不自动补撤/双撤循环**，台账保留 pending；
- **F6 连续异常自动降级只读对账（用户裁定）**：Windows 下连续 N=2 次 F6 链路异常即后台拉起 xiadan **只读**对账（复用 manual_audit.collect_broker_data，只读不写，差异 CRITICAL），1800s 冷却防反复弹交易端；另有**近 20 次 F6 检测质量滑窗**，异常率 ≥30% 每日 WARNING 一次（捕捉"时好时坏"的链路劣化，区别于连续异常降级），非 Windows 短路不计数；
- **trace_id/round_id（R1）**：每笔下单生成 `YYYYMMDD-8位hex` 的 trace_id，order_request/order_result/order_failed 三事件同链可检索，round 由调度器注入决策层。

### 6.2 纸面通道

[PaperTrader](file:///d:/Awakening/trader/trader.py) 与实盘**过完全相同的 RiskController**（kill_switch/时段/限买/限额/T+1），收 queue_only 参数、台账即时记 filled，可在 paper 模式演练全套状态机；成交后才改持仓，任何分支不允许绕过 trader 直接写 positions（09-10 严重漏洞修复项）。

---

## 七、风控体系（[risk_control.py](file:///d:/Awakening/trader/risk_control.py)）

| 项 | 规则 |
|---|---|
| 人工全停开关 | `logs/kill_switch.flag` 存在即拒**一切**下单（买入/卖出/F8 全拦，风控最高优先级）；启动恢复门或人工可置位，**只能人工删除**解除 |
| 买侧熔断标志 | `logs/kill_buy.flag`（JSON：时间/原因/指数/低点/日期）：大盘 -4% 自动写，**只拦新买入；卖出与 F8 撤单照常（风控止损永不停）**；指数自熔断低点回升 ≥1% 自动解除并通知，同日可再触发，次日自动清除，进程重启从 flag 恢复跟踪指数与低点；解析失败保持熔断等人工核查 |
| 交易时段 | 工作日 09:25–11:30 / 13:00–15:00（9:25 后可挂连续竞价单） |
| 每股每日限买 | **按成交计 1 次**（撤单不占名额）；卖出不限 |
| T+1 | 当日买入次日才能卖（读 entry_time）；positions 读取异常维持放行，由券商端最终校验（2026-09-15 用户裁定，不做 fail-closed） |
| 尾盘主动买截止 | 工作日 **14:55:00 后拒新主动买单**（含二次挂买，2026-09-16 用户裁定）；卖单/止损/F8/14:57 queue_only 结算单不受影响；`buy_deadline_enable` 可关 |
| 单票重挂上限 | 每票每方向每日重挂 ≤**3 次**（首次挂单不计；上笔 canceled/unknown 后再发才计数，买卖分别计，2026-09-16 用户裁定）；达上限规则性拒单，queue_only 旁路；`max_requeue_per_day=0` 关 |
| 日下单总量 | ≤20 单（**仅统计买入订单**；卖出止损不受此限额） |
| 单笔数量 | ≤1000 股（form 通道才有意义） |
| 满仓 | 最多 4 只，满仓只记录信号不开新仓 |
| 排队单 | 跌停卖/尾盘竞价单不受超时撤单限制；14:57–15:00 收盘集合竞价 F8 硬禁 |
| 大盘系统性风控 | 每轮轮头查**双指数**（主 sh000001 上证 + 副 sz399006 创业板指，任一跌得更深按其判定，[market_guard.py](file:///d:/Awakening/monitor/market_guard.py)）：跌幅 ≤-3% WARNING 当日一次；≤-4% 写买侧熔断 kill_buy.flag + CRITICAL（扫描继续、卖出照常）；回升 1% 自动解除（[market_crash_recovery_pct](file:///d:/Awakening/config.yaml)，0=当日不自动解除）；行情失败降级跳过绝不误熔断；risk 段 `market_guard_enable/market_index/market_index_2/market_warn_pct/market_crash_pct/buy_halt_file/market_crash_recovery_pct` 可配 |
| 状态持久化 | tmp+replace 原子写，防进程杀截断致计数清零；跨日自动重置 |

---

## 八、盘前自检与系统自检（[preflight.py](file:///d:/Awakening/preflight.py)）

**两套检查**：

1. **完整自检 run_preflight**（`python main.py preflight` 或 auto_round 启动自动跑，30–60s，调真实 API），三级 BLOCK/WARN/INFO，BLOCK 不过 `sys.exit(1)` 拒绝开盘并推送：ADB 设备（异常先 `adb start-server` 重试）、hexin 进程、快捷键面板（不可见自动唤出）、positions 可写、kill_switch 未触发（以上 BLOCK）；Cookie 有效、屏保、F12 自动登录、告警通道（WARN）；次新缓存预热、交易时段、**大盘行情源+风控阈值**（INFO，双源都失败才 WARN）；磁盘空间（<2GB BLOCK/<5GB WARN）。每项独立 try，单点失败不影响其余。
2. **轻量探活 run_health_checks**（1.9s、11 节点、零副作用：不连交易窗/不 F12/不推送/不写持仓/不调热榜），供 Web 台 `/api/health` 调用（15s 缓存，?force=1 强刷）；含"手机同花顺 App 在前台"（dumpsys 匹配包名/Activity）与"监控心跳"（今日文件+PID 存活+年龄判卡死；今日未启动=INFO 非故障）。

---

## 九、监控、告警与运维保障

- **心跳+看门狗**：主线程 touch，后台 30s 写 `heartbeat.json`；超 90s 不 touch 停写（挂死检测），独立 watchdog 进程 120s 未更新即 CRITICAL 推送；
- **看门狗带护栏自动重启（2026-09-16 用户裁定，[heartbeat.py](file:///d:/Awakening/monitor/heartbeat.py)）**：确认主进程 PID 已死亡后，护栏全部通过才自动拉起 auto_round 且看门狗继续值守等新心跳——① 当日重启 **≤3 次**（`logs/watchdog_restart.json` 计数，跨天自动清零）；② `kill_switch.flag` 人工全停期间**不重启**，`kill_buy.flag` 买侧熔断**不阻止**重启；③ 仅工作日 14:57 前重启（让路尾盘结算）；④ 重启前先跑 `main.py preflight --dry-run`，returncode≠0 放弃并告警；⑤ 拉起后 120s 宽限期内不重复拉起；⑥ 每次重启/中止均 CRITICAL 推送；`watchdog_restart_enable` 可关；
- **行情时钟守卫 ClockGuard（2026-09-16，[clock_guard.py](file:///d:/Awakening/monitor/clock_guard.py)）**：双源行情快照新增源成交时刻 `quote_ts`（腾讯 f30 紧凑时间 `YYYYMMDDHHMMSS` 已实测确认、新浪 f30 日期+f31 时间，解析失败安全回退本机接收时刻）与本机 `local_ts`；偏差绝对值 >30s 判时钟漂移、行情滞后 >120s 判陈旧，仅工作日 09:10–15:05 检测，边沿去重（开始告一次/恢复记一次），**纯 WARNING 不阻断交易**；阈值与开关可配；
- **跨进程单实例锁**（2026-09-15 新增，[instance_lock.py](file:///d:/Awakening/instance_lock.py)）：auto_round 与 pipeline_round 启动即抢同一把 `logs/app.lock`，第二实例打印双开风险（重复发键/持仓读改写互相覆盖）后 `exit(1)`，锁获取在任何设备操作之前。Windows 用命名互斥量 `Local\AwakeningTrader`（CreateMutexW），macOS/Linux 用 `fcntl.flock`，进程崩溃/被杀由 OS 自动释放，无陈旧 PID 锁问题；web/scan/watchdog/人工对账不持锁，可同跑；POSIX 锁文件写入持有方 PID 与启动时间供诊断；
- **日志按大小轮转**（2026-09-15 新增）：主进程与 pipeline 的文件日志统一改 RotatingFileHandler，单文件 10MB、保留 3 份（.1/.2/.3），杜绝长跑日把磁盘写满；cleaner 白名单同步纳入轮转份与 pipeline 日志按 TTL 清理；
- **运行资源守卫**（2026-09-15 新增，[resources.py](file:///d:/Awakening/monitor/resources.py)，零第三方依赖）：每轮轮头查磁盘余量 / logs 目录体量 / 进程峰值内存，**只告警不改变任何交易行为**，边沿触发（同级异常只推一次、级别升级补推、恢复推一条）：磁盘 <2GB（与 preflight 阻断线一致，持仓原子写/截图会失败）走 CRITICAL 手机推送，<5GB / logs>500MB / 内存峰值>800MB 走 WARNING；内存探测 POSIX 用标准库 resource、Windows 用 ctypes psapi，任一探测失败只跳过该项；阈值在 monitor 段可配、`resource_check_enable` 可关；
- **告警分级去重**（[notifier.py](file:///d:/Awakening/notify/notifier.py)）：CRITICAL【紧急】冷却 60s、WARNING【警告】/INFO【提醒】300s，按标题去重防设备反复掉线刷屏；超 100 条自动清理；企业微信（主，无条数限制）+飞书双推；推送失败本地 alerts.jsonl 照记；
- CRITICAL 场景：设备断连/看门狗/看门狗重启中止或自动重启/F8 核对失败/F6 只读对账差异/挂单状态不明/卖出未成交/封单大减/收盘竞价禁撤留单/大盘买侧熔断/磁盘空间危急/收盘复查异常/成交后持仓写失败/云自选删除护栏中止；
- **防睡眠**：运行期不休眠不熄屏；屏保进程检测并明确报错（会封锁模拟输入）；
- **垃圾清理 TTL**：截图 48h / scan 日志 14d / 诊断 png+xml 7d / 轮次档案 90d / 事件审计 1 年；状态文件永不清理；`python main.py cleanup --dry-run` 预览，auto_round 每日首轮 daily_guard 执行一次；
- **午盘日报（上午）**：11:30–13:00 幂等推送：上午大盘（上证/深证成指/创业板指涨跌幅、全 A 涨跌平家数、领涨板块 5，东财+双源行情、失败逐行降级）、持仓上午收盘价+浮盈、**上午成交流水（时间/方向/代码/名称/成交价）**、轮次/覆盖率/信号/告警；
- **收盘日报（全天）**（[report.py](file:///d:/Awakening/monitor/report.py)）：15:05–15:30 幂等推送：全天大盘（另加领跌板块 3）、**成交明细分上午/下午两组（含 15:00 尾盘补账）**、台账终态（买成/卖成/撤单/挂单中/状态不明）、尾盘竞价、持仓收盘价+浮盈、轮次/信号/告警；落盘 `logs/closing_report_YYYYMMDD.txt`；
- 市场数据模块 [market.py](file:///d:/Awakening/ths/market.py)：指数走行情双源，涨跌家数走东财 ulist.np（沪深 f104/f105/f106 单请求相加），板块走东财行业榜；push2 多子域轮试防间歇断连。

---

## 十、持仓安全：人工对账与启动恢复门

- **持仓唯一事实源** positions.json：写前 load 重读合并（tmp+replace 原子写），修复多实例读改写竞态；文件损坏立即隔离 `.corrupt-时间戳` 备份，随后自动尝试自愈（见下），无任何重建数据源时才置只读拒写；
- **损坏自愈：滚动快照 + 事件重放**（2026-09-15 新增，[positions.py](file:///d:/Awakening/models/positions.py)）：每次成功写持仓后原子刷一份 `logs/positions.snapshot.json`（ts/date/持仓列表）。positions.json 解析失败时：① 原件隔离 `.corrupt-*`；② 有快照则以快照为基线，仅重放 ts 严格晚于快照的 `position_add/position_remove` 交易事件（signal 等非 trade 事件忽略，跨天文件按日期裁剪），无快照则全量重放当日事件文件；③ 重建成功原子写回事实源并记 `positions_rebuilt` CRITICAL 审计事件，读写恢复；④ 无快照且无任何事件则维持 `_corrupt` 拒写（不凭空重建空仓覆盖事实源）。快照 ts 取自主文件写成功之后、审计事件在锁外写，故"只重放 ts>快照 ts"不漏不重；
- **人工对账**（[manual_audit.py](file:///d:/Awakening/trader/manual_audit.py)）：人工买卖后点 Web 按钮或 bat，连 xiadan 读当日成交/持仓，筛系统外成交，逐条"HH:MM 人工买入 XX N 股@价"确认后同步，差异先报告再改账；**F6 连续 2 次异常时复用该采集器做自动只读对账**（2026-09-16 用户裁定）：后台拉券商成交/持仓与台账比对，只读不写、差异 CRITICAL 人工介入，1800s 冷却；
- **启动恢复门**（仅 auto）：崩溃重启后先对账券商真实持仓+未完成委托，不一致或有遗留挂单即**写 kill_switch 熔断自动交易**+CRITICAL 告警，扫描照常，等人工处理；
- xiadan 操作经验固化：连接前按 PID+标题 set_focus 恢复托盘隐藏窗（否则 connect 抛 No windows found）；显式覆盖 `_main` 为"网上股票交易系统5.0"（否则解析到 IE 辅助隐藏窗）；唯一可靠输入是 pywinauto select()+type_keys()；网格 Ctrl+C/Ctrl+S 生产路径禁用（触发验证码保护/静默失败），持仓以 positions.json 为准。

---

## 十一、Web 可视化监控台（http://127.0.0.1:8899）

标准库 http.server + 原生 JS 单页，零新依赖，可与 auto_round 同机同跑（只读状态文件）。当前 **12 个页签**：

1. **总览**：模式/轮次/心跳/持仓与信号汇总；风控状态双徽章——红色"全停"（kill_switch，买卖全拦）与琥珀色"买侧熔断·只禁买入"（kill_buy，卖出/撤单照常、回升 1% 解除、次日清除），横幅并列展示解除方式；
2. **交易闸门（R6，2026-09-16 新增，[trading_gate.py](file:///d:/Awakening/monitor/trading_gate.py)）**：纯只读聚合 `/api/gate`，一页回答"现在为什么能/不能买、卖、撤"——买/卖/F8 三向 allowed+blockers（kill_switch、买侧熔断、非工作日/时段、14:55 买截止、日 20 单额度、14:57 收盘竞价）、当前阶段、尾盘截止、当日买入计数、看门狗当日重启次数/此刻是否满足重启护栏、主进程心跳新鲜度，以及当日六项新守卫触发计数（价格越限/滑点样本/F6 只读对账/F6 质量告警/收盘禁撤留单/云自选删除护栏），8s 轮询；
3. **系统自检**：横幅 block/warn/ok + 11 节点卡片（色条/图标/等级/详情，失败展开处理步骤），仅该页活动时 30s 轮询；
4. **持仓**：实时行情+盈亏；
5. **挂单状态**：挂单台账终态十列表（方向/状态色块/首挂价/挂单时间/是否持仓/现价/相对首挂价偏移/涨跌停封板/停牌/数据源），pending/unknown 置顶+熔断横幅（kill_switch 红 / buy_halt 琥珀双态）+五张统计卡，8s 轮询（`/api/attempts`，返回新增 buy_halt 字段）；
6. **扫描结果**：永远显示 mtime 最新一轮（verify_*/auto_round_*/pipeline_* CSV 统一数据源）；
7. **首次信号**：当日首多/首空卡片统计+时间轴表格，8s 轮询；
8. **信号核对**：verify_round 逐只截图人工核对（截图为扫描本存调试产物，零额外耗时），点图大图；
9. **满仓观察**：full_signal 事件；
10. **交易记录**：events JSONL 交易状态机（order_request/order_result/pending/closing 全阶段，2026-09-16 起事件携带 trace_id/round 可串联检索）；
11. **人工对账**：页面按钮触发采集+逐条确认（全局锁防连点双开 xiadan）；
12. **告警**：alerts.jsonl。

安全：仅绑 127.0.0.1；POST 必须 application/json + Origin/Referer 同源校验（防 CSRF）；date 参数 8 位数字白名单防路径穿越；所有响应 no-store。

---

## 十二、信号质量回溯

[report/signal_review.py](file:///d:/Awakening/report/signal_review.py)：统计历史多/空信号后 1/3/5 日胜率（首份报告样本：多头 1 日胜率 66.7%），基于事件档案（保留 1 年）复盘，为后续卖出逻辑扩展提供数据基础。

---

## 十三、CLI 命令速查

| 命令 | 用途 |
|---|---|
| `python main.py auto_round` | 生产主循环（自检→热榜→扫描→交易→云同步→尾盘结算） |
| `python main.py preflight` | 盘前完整自检（BLOCK 不过拒开盘） |
| `python main.py web` | 可视化监控台（--port 改端口） |
| `python main.py manual_audit --side buy/sell/both` | 人工对账 |
| `python main.py watchdog` | 独立看门狗进程（auto_round 会自动拉起） |
| `python main.py cleanup [--dry-run]` | TTL 垃圾清理 |
| `python main.py wl_check` | 云自选 Cookie 诊断（不需手机） |
| `python main.py selftest` | 交易规则回归集（tests/test_rules.py，**224 项断言**，离线可跑，S0–S14 十四章） |
| `python main.py scan_loop / test_current` | 纯扫描 / 只测当前页 |
| `python pipeline_round.py --no-buy --after-hours` | 生产对齐流程（盘后同步+全扫不交易） |
| `python verify_round.py` | 核对轮：写 verify_*.csv + 逐只截图供 Web 人工核对 |

---

## 十四、待验证 / 使用前注意

1. **盘中真实成交闭环**：热键委托/撤单链路盘后已通，F6 正向成交回报与滑点待开盘验证；通过后将 config.yaml `execution.mode: paper` 改 `auto`；
2. **每日首次**：必须先 F12 登录并关闭交易窗口，F1–F4 才生效（系统 bootstrap 已自动做，失败会拦）；
3. ScreenSaverPlayer 等屏保若运行必须人工退出；
4. verify_enable 默认 false（不弹 xiadan 回查、避验证码），成交验证走 F6；
5. 当前账户为模拟账户（09-08 重置，14.18 万空仓），paper→auto 切换前再次确认。
6. **第二轮新增的 Windows 专有链路待实盘环境验证**（Mac 开发侧 selftest 224 项全绿、非 Windows 短路）：卖出侧 F6 预检、F8 撤单前后代码/F6 核对、F6 连续异常拉起 xiadan 只读对账、看门狗带护栏重启 auto_round（preflight 子进程 + DETACHED 拉起）、价格守卫真实滑点采样；行情时间字段已在 Mac 真机实测（腾讯 f30=`20260916114435` 紧凑格式，ClockGuard 解析无静默失效风险）。
7. **尾盘顶格热键实盘绑定确认（2026-09-16 新规则前置条件）**：用户须先在同花顺行情端"自定义快捷键"中把"涨停价买入（25% 仓位）/跌停价卖出（清仓）"绑到两个物理键并填入 `hotkey.closing_buy_limit_key`/`closing_sell_limit_key`；未填则结算单回退 F1/F3 最新价并 WARNING。需在 Windows 端实测：① 该客户端确实支持顶价键自定义且数量语义正确；② 14:57 结算单实际发出的键位与撮合结果（15:00:05 F6 复查）；③ 顶格单实际按收盘价成交而非涨跌停价。

---

## 十五、后续优化项

### 2026-09-15 外部评审首轮整改（已落地，selftest 147/147 全绿）

输入：外部十模型评审文档（豆包/千问/元宝/文心/ChatGPT/智谱/讯飞/混元/Kimi 九份点评 + 内嵌 OLE《生产级自动交易系统优化研究报告》七章），逐条对照代码核实后按"高共识、零交易行为变化优先"落地，规则冲突项均经用户裁定：

1. ✅ **大盘风控分级（用户裁定）**：-4% 由旧"一刀切 trip_kill_switch（卖出一并冻结）"改为**买侧熔断** `kill_buy.flag`——只禁新买入，卖出/F8 撤单照常（风控止损永不停）；指数自熔断低点回升 1% 自动解除并通知、同日可再触发、次日自动清除、重启从 flag 恢复指数与低点；`kill_switch.flag` 保留为人工全停（买卖全拦、只人工删）；双指数（上证+创业板指）取跌幅最深者；
2. ✅ **收盘集合竞价 F8 硬门禁（合规硬规则，用户裁定）**：工作日 14:57:00–15:00:00 封单开板/骤减只告警，系统硬拦截 F8、挂单留到 15:00 收盘对账，每票一条 CRITICAL；
3. ✅ **跨平台单实例锁**（[instance_lock.py](file:///d:/Awakening/instance_lock.py)）：防多开 auto_round/pipeline 双发键、持仓读改写互覆盖；Windows CreateMutexW 命名互斥量 / POSIX fcntl.flock，进程死亡 OS 自动释放；只锁真实发键的两个入口，web/scan/watchdog/对账豁免；
4. ✅ **日志轮转**：main/pipeline 统一 RotatingFileHandler（10MB×4），cleaner 白名单补齐轮转份与 pipeline 日志 TTL；
5. ✅ **持仓损坏自愈**：滚动快照 + 交易事件重放自动重建写回（见 §10），原 .corrupt 隔离拒写作为无数据源时的兜底不退化；
6. ✅ **零依赖运行资源守卫**（[resources.py](file:///d:/Awakening/monitor/resources.py)）：磁盘/logs 体量/进程内存峰值，边沿触发只告警，磁盘 <2GB 走 CRITICAL（与盘前阻断线一致）；
7. ✅ Web 总览/挂单页新增买侧熔断琥珀色徽章，`/api/attempts` 与 risk_state 返回 `buy_halt` 字段。

### 2026-09-16 外部评审第二轮整改（已落地，selftest 147→**206 项全绿**，连跑 10 轮无 flaky）

输入同上（十模型点评 + 内嵌七章报告）的第二轮逐条核实。**8 项规则冲突全部经用户人工裁定后落地**，原话留痕：

1. ✅ **发键前价格守卫——裁定"先只告警+采集滑点（推荐）"**：买 +0.8%/卖 −1.5% 越限仅 WARNING + price_guard 台账/事件，照常发单；预留 `price_guard_enforce` 硬拦开关；queue_only 与跌停排队卖跳过；
2. ✅ **F8 撤单前代码核对+撤单后 F6 验证——裁定"增加，失败只告警（推荐）"**：失败仅 CRITICAL 人工介入，绝不自动补撤/双撤循环；
3. ✅ **F6 连续异常——裁定"自动只读对账（推荐）"**：连续 N=2 次降级 xiadan **只读**对账（复用 manual_audit.collect_broker_data，只读不写、差异 CRITICAL，1800s 冷却）；另加近 20 次滑窗异常率 ≥30% 每日质量 WARNING；
4. ✅ **卖出侧 F6 预检——裁定"增加（推荐）"**：券商无持仓拒卖，检测异常 fail-open；
5. ✅ **看门狗自动重启——裁定"自动重启（带护栏，推荐）"**：日 ≤3 次；kill_switch 期间不重启；14:57 后不重启；重启前先跑 preflight（returncode 必须 0）；重启 CRITICAL 通知；**kill_buy 买侧熔断不禁重启**；120s 拉起宽限防重复拉起，重启后看门狗继续值守；
6. ✅ **尾盘新买单截止——裁定"14:55（推荐）"**：卖单/止损/F8/14:57 queue_only 结算单不受影响；
7. ✅ **单票每日重挂次数上限——裁定"增加（每票 3 次，推荐）"**：首次挂单不计，买卖分别计，可配可关；信号寿命不限；日 20 单总额度不变；
8. ✅ **云自选同步删除护栏——裁定"中止+告警（推荐）"**：待删除占现有云自选 >30% 即中止整表替换 + CRITICAL，v1/v2 两条删除路径之前拦截；持仓股本不删。

**D 类（默认值=现行行为、可回滚、纯标准库）同步落地**：R1 trace_id/round_id 贯穿下单三事件；R2 配置 schema 启动校验（枚举/区间/时间格式，默认 warn-only，未知键不再静默丢弃）；R3 client_order_id 幂等键 + 台账状态机收紧（filled 终态、非法流转拒绝）；R5 扫描连续切页失败新增 `stuck_policy=skip` 跳过续扫（默认 abort 不变）；R6 TradingGate 只读聚合 + Web"交易闸门"页（见 §11）；滑点/延迟质量采集；ClockGuard 时钟偏差/行情陈旧只告警（30s/120s，腾讯 f30 格式已真机实测）。另修复一处测试侧时钟依赖（日报用例台账时间固定 14:30，上午跑 selftest 不再误红）。

### 2026-09-16 尾盘集合竞价顶格报价（用户新增规则，selftest 206→**224 项全绿**）

用户规则原话："尾盘竞价阶段如果确定买入直接报价涨停价买，如果确定卖出直接报价跌停价卖，确保买入和卖出必定成功。"落地前排查发现两个硬事实，经用户两项裁定：

1. ✅ **通道——裁定"配置专用热键（推荐）"**：F1/F3 热键由客户端按最新价发单、程序无法指定价格，故新增 `hotkey.closing_buy_limit_key`/`closing_sell_limit_key` 两个**用户在同花顺面板自定义的顶价键位**（如 {F9}/{F10}）；尾盘结算单（Action 新增 `closing_auction` 标记，scheduler 两支构造处置位，经决策层→trader 全链路透传）改发顶价键，**数量仍由客户端控制（25%/清仓语义不变，不做系统侧股数计算）**；未配置键位时回退 F1/F3 并按方向 WARNING 一次（不阻断结算），`closing_limit_key_enable=false` 可整体关闭，schema 校验键位格式；盘中普通单与盘中跌停排队卖（queue_only 但非 closing_auction）完全不受影响；order_sent 事件带 `closing_auction` 字段可检索；
2. ✅ **封板物理例外——裁定"维持跳过+告警（推荐）"**：向用户说明集合竞价顶格单按**收盘价统一撮合**（并非按涨跌停价成交，顶格只决定撮合优先级），但涨停封死的票买单、跌停封死的票卖单对手盘为空无法保证成交：涨停封板买单维持不挂（跳过+开板告警人工补，既有逻辑），跌停封板卖单维持排队+封单监控（既有逻辑）。
3. 测试：新增 S14 整章 18 项（默认值/键选择/未配置回退与告警去重/开关关闭/schema 格式/决策层透传/scheduler 两支自动置位），同时修复 S13.7 ClockGuard 三处 `time.time()` 运行时刻依赖（固定工作日 10:30，盘后/周末跑 selftest 不再假 FAIL，与 S9 同类修复）。

### 2026-09-18 第二版十模型点评采纳（selftest 224→**256 项全绿**，连跑 3 轮无 flaky）

依据《十大模型点评（9.16第二版）》（豆包/智谱/混元/DeepSeek/星火/ChatGPT 等共十份，含 4 份 OLE 嵌入文档）逐条核对现状代码后落地。遵循既定约束：零新重型依赖、默认值=现行行为/新功能默认关、既有断言只增不改（新增断言开 S15 整章 32 项）。共采纳 16 项：

**交易/风控护栏（默认安全，行为不回退）**

1. ✅ **大盘买熔断解除冷却防抖**：[market_guard.py](file:///d:/Awakening/monitor/market_guard.py) `risk.buy_halt_recover_cooldown`（默认 0=立即解除，现行行为不变）；配置后首次回升达阈先进入观察，期间再跌穿则取消解除继续熔断，持续站稳才解除并推送【回升观察】；
2. ✅ **云删除护栏两段式二次确认**：[watchlist.py](file:///d:/Awakening/ths/watchlist.py) `delete_guard_confirm_enable`（默认关）；超 30% 触发后，未到 `hard_pct`(60%) 硬顶则等待 `confirm_wait`(45s) 后 fresh 重拉，差集≤`max_diff`(2) 只判为瞬时抖动放行，否则/异常维持中止；持仓股任何路径不删除；二次确认写 anomaly 事件；
3. ✅ **持仓浮亏只读告警**：[scheduler.py](file:///d:/Awakening/scanner/scheduler.py) `positions.loss_alert_enable`（默认开）；用 entry_price vs 腾讯现价，≤-10% WARNING/≤-15% CRITICAL，每股每档当日一次，只告警不自动止损；
4. ✅ **unknown 挂单悬置超时升级**：[risk_control.py](file:///d:/Awakening/trader/risk_control.py) `scan_unknown_stuck()`，按 attempt ts 计龄，15min 升 CRITICAL/60min 补日报标记，标记随台账持久化、离开 unknown 清除；主循环轮头调用并推送；
5. ✅ **T+1 读取异常写 anomaly 审计**：`_is_t0_position` 读 positions 失败时留痕（fail-open 语义不变，不锁死卖出）；
6. ✅ **F8 核对 fail-open 滑窗告警**：[hotkey_trader.py](file:///d:/Awakening/trader/hotkey_trader.py) `cancel_fail_alert_enable`（默认开）；撤单前标题解析不出仍照常撤，但 1800s 滑窗内累计 3 次则 CRITICAL（计数回落自动重新武装）；
8. ✅ **发键白名单拦截+审计**：`key_whitelist_enable`（默认开）；内置 F1–F12 并并入 config 显式配置的全部键位（含尾盘顶格自定义键），非允许键发键前拦截、不耗买入名额并写 anomaly；`allowed_keys` 可自定义；
10. ✅ **重挂达限专门审计事件**：pre_check 重挂达限拒绝处写 `requeue_cap_reject`（每股当日一次）；
11. ✅ **schema 分级 strict 开关**：[config.py](file:///d:/Awakening/config.py) `strict_violations`；validate_config 支持 tagged 返回 `(section,msg)`，`schema.strict_enable`（默认关）开启后 risk/seal/timing 段越界由 main 启动时拒绝运行，枚举类（空段）问题仍只 WARNING；
12. ✅ **15:00 收盘复查三次退避**：`closing_recheck_delays=[5,600,1800]`；前两次 F6 链路异常只退避重试（写 closing_check_retry），末次仍异常才转 unknown+CRITICAL，避免盘后瞬时卡顿误判；
13. ✅ **卖出超日单量只 WARNING**：新增 `sell_count`（SELL 发单即计），超 max_orders_per_day 写 `sell_budget_warning` 但绝不拦卖（空头必卖）；
14. ✅ **非持仓 unknown 显式事件 + 尾盘结算幂等键**：[decision.py](file:///d:/Awakening/decision/decision.py) 未持仓 unknown 不交易但写 `nonheld_unknown`（每股当日一次）；尾盘结算挂单前校验 `settle:YYYYMMDD:code:side`，防窗口内异常重试重复挂单；
15. ✅ **重放游标字节偏移 event_cursor**：[audit.py](file:///d:/Awakening/models/audit.py) EventLog.cursor 返各事件文件字节数（自有锁内测长度），快照带 cursor；[positions.py](file:///d:/Awakening/models/positions.py) 重放支持 f.seek 字节偏移，旧快照无 cursor 回退 ts 比较并 WARNING，向前兼容；
9. ✅ **每日关键数据 zip 备份**：[backup.py](file:///d:/Awakening/cleanup/backup.py) `backup.enable`（默认关）；打包 positions/trade_state/positions.snapshot/config.yaml/当日 events/当日两份日报，retain_days(30) 滚动清理，默认排除 ths_cookie.txt；auto_round 每日首轮执行一次。

**P0 项默认关闭（触碰既有裁定路径，仅旁路不改语义）**

- ⏸ **撤单竞态撤前 F6 终核**（智谱 A2）：`cancel_race_check_enable` 默认关；开启后 F8 前先 F6 核持仓，疑似已成交则不发 F8 只 CRITICAL（不自动补撤/补账）；F6 链路异常仍 fail-open 照常撤（现行行为不变）；
- ⏸ **DegradedGuard 手机通道降级、卖出双帧确认**等高时延项：维持路线图，待实盘窗口数据后评估。

**本批明确不采纳（留痕，模型可复审）**

- 止损/止盈/账户日亏自动停（用户裁定）；系统侧股数计算（维持 F1/F3 客户端语义）；T+1 改 fail-closed；凭证加密/外置；信号时效衰减；kill_switch 放行 F8；partial 成交状态、hotkey_trader 大拆分/金样本/CI（列路线图）；以及 Prometheus/Grafana/SQLite/gRPC/pydantic/psutil、WiFi-ADB 等与单机单账户阶段不匹配的重型基建；星火中已被后续版本修复的过时低质量条目不落地。

### 短期（实盘验证前后，优先级高）

1. ✅ **已落地 2026-09-12** 尾盘结算的扫描冲突窗口：scan_loop 主循环加 14:57:00 deadline 中断点（`should_break` 回调，持仓插扫之前生效），中断轮跳过覆盖率审计/云同步提前返回，run_round 后立即补触发尾盘结算；
2. ✅ **已落地 2026-09-12** 挂单台账可视化：Web 第 4 页签"挂单状态"（`/api/attempts`，十列+状态色块+熔断横幅+统计卡，pending/unknown 置顶，8s 轮询）；
3. ✅ **已落地 2026-09-12** 停牌/临停显式识别：快照 `halted` 标志，决策层买 WARNING/卖 CRITICAL 拦截且当日去重，尾盘结算两支同样拦截，复牌后自动恢复；
4. ✅ **已落地 2026-09-12** 收盘日报：15:05–15:30 幂等推送（台账终态/尾盘竞价/持仓收盘价浮盈/轮次信号告警），落盘 closing_report_YYYYMMDD.txt；
5. ✅ **已落地 2026-09-12** 行情源冗余：腾讯主+新浪备自动切换（快照带 source/halted），已真机验证两源现价一致；指数代码（sh000001）直传。

### 中期（策略与执行增强）

6. **卖出逻辑体系扩展**（用户已明确空头只是最晚信号）：在不动摇"空头必卖"前提下叠加量能/破位等卖出信号，决策层 `_decide_sell` 已预留分支结构；每个新信号需像多空角标一样先做历史胜率回溯（signal_review 框架可复用）；**止盈线/移动止损/账户日亏损自动停经 2026-09-15 用户裁定暂不做**（原话："不需要，当后续卖出规则多了自然会卖出的"）；
7. ⏸ **用户裁定维持现状（2026-09-12）**：仓位以行情端快捷键为主（25%/清仓），兜底 xiadan 界面已**手动设置为买入按金额、卖出按股数**；不做系统侧精确股数/资金通道；
8. ⏸ **用户裁定维持告警（2026-09-12）**：涨停开板/封单异动只推送告警，人工判断是否补单；等用户校验经验丰富后再把补单策略写入系统，不做一键补单；
9. ✅ **已落地 2026-09-12，2026-09-15 扩至 147 项，2026-09-16 扩至 206→224 项** 系统化测试：[tests/test_rules.py](file:///d:/Awakening/tests/test_rules.py) 覆盖台账状态机/价格条件/时段/决策分支/queue_only/键位窗口/封单 10% 异动/收盘竞价 F8 门禁/停牌/双源/大盘买侧熔断全状态机/14:57 中断/两份日报/市场概貌/挂单页/单实例锁/日志轮转/持仓快照自愈/资源守卫/**配置 schema/14:55 买截止/重挂上限/同态幂等/云删除护栏/价格守卫/F6 质量滑窗/ClockGuard/交易闸门/trace_id/看门狗重启全护栏（S13 整章 44 项）/尾盘顶格热键+closing_auction 全链路透传（S14 整章 18 项）**，`python main.py selftest` 一键跑，无网络自动跳过联网用例，09-16 连跑 10 轮 206/206、S14 落地后连跑 3 轮 224/224 无 flaky；
10. ✅ **已落地 2026-09-12，2026-09-15 升级** 大盘系统性风控：MarketGuard 每轮轮头查双指数，-3% WARNING/-4% 自动熔断；**09-15 按用户裁定从 kill_switch 一刀切改为买侧熔断 kill_buy.flag（只禁买/卖出照常）+ 低点回升 1% 自动解除 + 次日清除 + 重启恢复**，preflight 有指数行情源探活；
11. 🔄 **规则已按用户口径更新（2026-09-12）**：封单阈值从"较峰值缩 50%"改为"**环比秒级变动 ≥10% 即告警、有挂单必撤**"（基数 50 手保护、180s 同票冷却、10s 轮询）；阈值的回测自校准仍待封单序列数据积累后再做。

### 评审后纳入路线图（模型高共识、待编码，按优先级）

R1. ✅ **已落地 2026-09-16** 全链路 trace_id/round_id：每笔下单 `YYYYMMDD-8位hex` 串联 order_request/order_result/order_failed，round 由调度器注入，Web 交易记录页可按 trace 检索；
R2. ✅ **已落地 2026-09-16** 配置 schema 启动校验：[config.py](file:///d:/Awakening/config.py) validate_config 校验枚举/取值区间/时间格式（mode/channel/sync_mode/stuck_policy/变体/截止时间/重挂次数/护栏比例/时钟阈值等），未知键收集为 warn（默认只 WARNING 不阻断，杜绝"拼错配置项看似生效实则默认值"）；
R3. ✅ **已落地 2026-09-16** 订单幂等键 + attempts 状态机收紧：cid 幂等键（见 §3.4），pending/canceled/unknown/filled 合法流转显式化，filled 终态硬拒回退，同态重复幂等静默；
R4. ✅ **已落地 2026-09-16** F6 链路故障自动降级 xiadan **只读**对账（用户裁定，见 §6.1/§10），连续 2 次异常触发、1800s 冷却、只读不写差异 CRITICAL；
R5. ✅ **已落地 2026-09-16** 扫描连续失败跳过续扫：`monitor.stuck_policy=skip` 时连续切页失败只跳过该股续扫（默认 abort 维持熔断语义）；
R6. ✅ **已落地 2026-09-16** 全局 Trading Gate 汇总视图：[trading_gate.py](file:///d:/Awakening/monitor/trading_gate.py) 纯只读聚合 kill_switch/kill_buy/交易时段/14:55 截止/日额度/收盘竞价/看门狗重启/心跳/当日守卫计数，Web"交易闸门"页 + `/api/gate`（见 §11）；
R7. ⏳ **信号双帧确认（未裁定，待用户决策）**：相邻两次截图角标一致才发键（降单帧误识别），需先实测对 8–10s/只买单链路的时延影响；
R8. ✅ **已落地 2026-09-16** 看门狗自动拉起：带护栏重启（日 ≤3 次/kill_switch 拦/14:57 前/preflight 通过/CRITICAL 通知，启动恢复门语义不变，见 §9）；
R9. ⏳ **两级漏斗选股后台采集器（接口已实测，待编码）**：第一级东财 clist 全市场粗筛（流通市值 f21/量比 f10/换手 f8，主板 `fs=m:1 t:2,m:0 t:6`，串行 0.4–0.5s/页），第二级候选股并行腾讯日 K 精筛（不含当日近 15 日最高点回撤等）；f21 午间快照切换偶发百倍脏数据，必须加分位/总市值-流通市值比值/stock 复核守卫；后台异步采集不阻塞主交易。

### 评审后明确不做 / 维持现状（用户裁定留痕，评审模型可复审）

- ⏸ **止损线/止盈线/账户日亏损自动停**：用户裁定不做——"不需要，当后续卖出规则多了自然会卖出的"；卖出规则随信号体系另行扩展（见中期 6）；
- ⏸ **T+1 读取 positions 异常改 fail-closed**：维持现状放行，由券商端最终校验（个人单机、避免文件抖动锁死卖出）；
- ⏸ **系统侧计算 25% 仓位股数**：维持热键客户端语义（中期 7）；**信号时效衰减**：明确不做（交易规则 5）；
- ⏸ **凭证加密/强制环境变量**：用户裁定个人单机使用明文可接受（"暴露在代码中也没关系"）；Notifier 已支持环境变量但不强制；
- ⏸ **Prometheus/Grafana、独立 OMS、gRPC 微服务、SQLite 迁移、WiFi-ADB、双人审批**：与当前单机单账户阶段不匹配的重型基建，不引入；监控走标准库 + Web 台 + 手机推送闭环。

### 长期（平台化）

12. **策略多信号融合**：多周期共振、量能过滤、板块联动等，把"单一角标"升级为可配置规则引擎（信号仍以角标为最高优先级买卖原则）；
13. **服务化部署**：脱离个人 PC 长会话（开机自启+崩溃守护已有雏形，可进一步做 Windows 服务/任务计划+状态页外网安全访问，当前 Web 仅 127.0.0.1）；
14. **多账户/多设备**：PaperTrader/HotkeyTrader 接口已统一，扩展为多风控实例+多手机通道；
15. **完整复盘体系**：交易记录+信号快照+封单事件+延迟指标（action.signal_ts 已全链路埋点）自动生成日/周复盘报告，持续迭代信号质量。

---

## 附：重大问题修复演进（审计留痕）

- **2026-09-10 全库审计**（48 文件约 6000 行）：修复 11 个真实问题——严重 2（paper 分支绕过风控盘后违规建仓；交易线程与挂单监控无窗口锁）、高 2（持仓文件损坏被空仓覆盖；F6 异常误判有持仓）、中 5（risk_block 标记不一致刷屏、定位失败白耗名额、CSRF、设备掉线死循环）、低 2+加固 1；
- **2026-09-11**：goto 静态坐标落页 bug（盘后横条挤 tab 致持仓巡检静默全 NONE，改动态定位+UNKNOWN 上抛）；多空角标"历史角标 vs 当日角标"语义裁定（中心带只读最新 K）；
- **2026-09-12 凌晨**：热榜三因对齐（88 只主板）、首次信号快照、系统自检可视化页、8 项优化（T+1 校验、告警 P0/P1/P2 分级去重、超时立即撤单、磁盘空间检查、ADB 自恢复、巡检去重、轮间隔自适应、F8/F5 分级撤单）、补买价格条件；
- **2026-09-12 晚（八大规则改造）**：挂单 attempts 台账状态机、二次挂单双向价格保护、涨停不排队/跌停排队+封单监控自动撤单、9:25 开盘窗口 5 分钟特殊等待、F3 取代 F4 默认卖出、14:57 尾盘集合竞价兜底结算+15:00 成交复查线程、queue_only 全通道透传、到期撤单全改 F8 防误撤排队单。39 项冒烟断言全过、全部模块编译与导入通过；
- **2026-09-12 晚（第二批 7 项优化落地）**：① scan_loop 14:57 中断点让路尾盘结算；② 行情腾讯/新浪双源冗余（快照新增 halted/source，修复收盘日报 ts epoch 浮点显示 bug）；③ 停牌显式识别+决策层/尾盘双拦截；④ 15:05 收盘日报；⑤ Web"挂单状态"第 11 页签（10→11）；⑥ 大盘系统性风控（-3%预警/-4%熔断，risk 段四项可配，preflight 探活）；⑦ tests/ 回归集固化（83 项断言，main.py selftest 入口）。83/83 全绿、12 个改动文件 py_compile 通过。中期 6/7/8/11 与长期 12–15 待策略研究/历史数据/基础设施，未动；
- **2026-09-12 用户裁定四件事（第三批）**：① 封单规则重定义——环比变动 ≥10% 秒级告警（骤减 CRITICAL/暴增 WARNING，180s 冷却，50 手基数保护），有 pending 挂单（买卖均算）必 F8 撤单每票一次，轮询 15s→10s，HotkeyTrader 新增 `_pending_side` 台账查询、cancel_fn 带方向；② 两份日报增强——新增 [ths/market.py](file:///d:/Awakening/ths/market.py)（沪深指数双源+东财 ulist 涨跌家数 f104/105/106+行业板块榜，push2delay/push2/82/83 四子域轮试），午盘报"上午大盘+上午成交"、收盘报"全天大盘（含领跌）+上下午两组成交流水+持仓浮盈"，真机渲染验证（5284 只/涨619 跌4619）；③ 涨停补单确认维持只告警；④ 仓位确认维持热键为主+xiadan 手动金额买/股数卖。回归集扩至 104 项全绿。
- **2026-09-15 外部评审首轮整改（第四批）**：输入为《点评第一版.docx》——九个大模型（豆包/通义千问/元宝/文心/ChatGPT/智谱/讯飞/混元/Kimi）对 09-12 版报告的点评，及文档内嵌 OLE 对象中的《生产级自动交易系统优化研究报告》（七章，docx 内 OLE2 偏移切 ZIP 提取）。经逐条对照代码核实（剔除基于旧版报告的过时建议）与四项规则冲突人工裁定后落地：① 大盘 -4% 熔断改**买侧熔断**（只禁买、卖出/撤单照常），新增 kill_buy.flag（JSON 含指数/低点/日期）、低点回升 1% 自动解除、同日可再触发、跨天自动清除、重启从 flag 恢复，kill_switch 保留人工全停；② 14:57–15:00 收盘集合竞价 **F8 硬门禁**（合规硬规则，只告警留单到 15:00 对账）；③ 跨平台**单实例锁**（Win 命名互斥量/POSIX flock，OS 自动释放）；④ 日志 **RotatingFileHandler** 10MB×4 + cleaner 白名单补齐；⑤ 持仓**快照+事件自愈重建**（.corrupt 隔离拒写保留兜底）；⑥ 零依赖**资源守卫**（磁盘/logs/内存，<2GB CRITICAL）；⑦ Web 买侧熔断徽章。回归测试 104→**147 项全绿**（新增买侧熔断 18 场景、收盘门禁 6 项、生产加固 14 项、资源守卫 10 项；期间修复测试侧 3 个自身缺陷：json 导入、后台线程竞态、陈旧 flag 断言顺序）。用户明确否决：止损/止盈/日亏损线、T+1 fail-closed、系统侧仓位计算、信号衰减、凭证加密强推及 Prometheus/OMS/gRPC/SQLite 等重型基建，均已在 §15 留痕。
- **2026-09-16 外部评审第二轮整改（第五批）**：在十模型点评 + 内嵌七章报告基础上做第二轮差距分析，**8 项规则冲突全部经用户人工裁定**（裁定原话见 §15 第二轮专章）：发键前价格守卫（告警+滑点采集，enforce 预留）、F8 撤单前后代码/F6 核对（失败只告警不双撤）、F6 连续 2 次异常自动降级 xiadan **只读**对账（复用 manual_audit，只读不写）、卖出侧 F6 预检、看门狗带护栏重启（日≤3/kill_switch 拦/kill_buy 不拦/14:57 前/preflight 必过/120s 宽限/CRITICAL）、14:55 尾盘主动买截止（结算单旁路）、单票每日重挂 3 次上限（首挂不计/买卖分计/queue_only 旁路）、云自选删除 >30% 护栏（v1/v2 删除前拦截）。D 类加固：R1 trace_id/round_id、R2 schema warn-only、R3 cid 幂等键+状态机（filled 终态/同态静默/跨态提示）、R5 stuck_policy=skip、R6 TradingGate+/api/gate+Web 第 12 页签、ClockGuard（30s 漂移/120s 陈旧只告警，腾讯 f30 真机实测为 `YYYYMMDDHHMMSS`）、F6 近 20 次滑窗质量告警。涉及 [risk_control.py](file:///d:/Awakening/trader/risk_control.py)、[hotkey_trader.py](file:///d:/Awakening/trader/hotkey_trader.py)、[heartbeat.py](file:///d:/Awakening/monitor/heartbeat.py)、[watchlist.py](file:///d:/Awakening/ths/watchlist.py)、[scanner.py](file:///d:/Awakening/scanner/scanner.py)、[scheduler.py](file:///d:/Awakening/scanner/scheduler.py)、[quote.py](file:///d:/Awakening/ths/quote.py)、[decision.py](file:///d:/Awakening/decision/decision.py)、新增 [clock_guard.py](file:///d:/Awakening/monitor/clock_guard.py) 与 [trading_gate.py](file:///d:/Awakening/monitor/trading_gate.py)。全程零新重依赖、新默认值等价现状、每项 flag 可回退、核心交易语义（空头必卖/F1 25%/F3 清仓/14:57 queue_only 结算）零变化。回归 147→**206 项全绿**（S0 新增默认值 11 项、S1b 尾盘截止 4 项、S13 整章 44 项；并修复 S9 日报用例依赖运行时刻的时钟 flaky——台账记录时间固定 14:30），连跑 10 轮无复现历史偶发失败；/api/gate 实测 200。剩余路线图：R7 信号双帧确认待用户裁定、R9 两级漏斗选股采集器待编码。
